{
  "name": "worker-example",
  "version": "0.1.0",
  "author": "DFST",
  "scripts": {
    "test": "jest",
    "local": "jest --chain=local",
    "devnet": "jest --chain=devnet --deploy=false",
    "deploy": "jest --chain=devnet --deploy=true"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "packageManager": "yarn@4.2.1",
  "dependencies": {
    "o1js": "^1.1.0",
    "zkcloudworker": "^0.5.6"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "slowTestThreshold": 1500,
    "testTimeout": 10800000,
    "bail": true,
    "verbose": true,
    "roots": [
      "tests"
    ],
    "setupFilesAfterEnv": [
      "./jest-config.ts"
    ]
  }
}
